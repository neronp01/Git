
import { ElementFinder } from 'protractor';

import { AppWebComponent } from './app-web.component';

describe('AppWeb: ', () => {
    describe('Events and Lifecycle', () => {
        beforeEach(() => {
            this.instance = new AppWebComponent();
        });

        it('Properties are defined', () => {
            // TODO
        });
    });
});
